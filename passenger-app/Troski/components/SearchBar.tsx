import {View, Text, TouchableOpacity} from 'react-native'
import React from 'react'
import {Ionicons} from "@expo/vector-icons";

const SearchBar = ({name, onPress} : any) => {
    return (
        <View></View>
    )
}
export default SearchBar
